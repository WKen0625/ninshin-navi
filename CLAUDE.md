# Tsugiraku（Tsugiraku Navi）— プロジェクト指針

作成日: 2026-09-18 ／ 段階1（世田谷区）向け

## 1. 何を作るか（1文）

住所・出産予定日・「手元にある紙」を入れるだけで、**今週やること・分娩予約の締切・実際の負担額** がわかるWebサイト。

ミッション: **いまの家族の迷いを減らし、その記録で次の家族を守る。**

## 2. 対象と展開順

| 段階 | 対象 | 2025年出生数 |
|---|---|---|
| 1 | 世田谷区（検証） | 約7,000 |
| 2 | 東京都 | 85,064 |
| 3 | 首都圏4都県（＋神奈川・埼玉・千葉） | 207,071 |
| 4 | ＋愛知・大阪・兵庫・京都・福岡 | 378,456（全国の56.4%） |

## 3. 設計原則（必ず守る。反する実装は止めて質問する）

1. **仕組みは1つ、地域の違いはデータで持つ。** 地域追加でコードを変えない。足すのは `data/` のYAMLだけ。
2. **答えはAIの自由回答ではなく、データ表から決定的に返す。** LLMは変更監視の差分要約にしか使わない。
3. **すべてのルール・金額に `source_url` と `verified_at` を持たせ、画面に表示する。** 無い行はseedで投入を拒否する。
4. **選べる地域は `data/service-area.yaml` が決める。いまは東京23区だけ**（2026-09-20 決定。それまでの「対象外の地域をつくらない」を改めた）。一覧に無い市区町村は選べず、「準備中。リクエストは info@tsugiraku.jp あてに」と案内する。一覧の中では、区のファイルがまだ無くても「国＋東京都」の骨格を表示し、未確認は画面に明示する。広げるときは一覧に足すだけで、コードは変えない。
5. **個人情報は最小限。** 氏名不要。健診結果を保存しない。写真アップロード機能を作らない。
6. **書類は一覧から選択。** 該当なしは「その他」→ `document_suggestions` に自由記述を保存し、人がレビューしてマスタに追加する。
7. **施設の予約の実態は、利用者の入力で溜めていく**（2026-09-20 決定）。「病院と締切」の各施設から、電話した結果・施設に言われた締切の週・分娩予約金を、選択式・任意・同意つきで記録できる。出すのは集計だけで、「施設の公式の情報ではない」と添える。**アンケートは手続きの完了チェック直後にも、任意で1問だけ。** 回答後に同じ地域の集計を返す（答えることが本人の得になる）。
8. **病院情報の画面に広告・製品を置かない。** 製品は別画面。PR表記必須。記録への謝礼は出さない。
9. **分娩費用は制度を必ず区別する。** `scheme` = `lumpsum`（出産育児一時金50万円）／`new_scheme`（2028年6月頃までに開始、施設の準備次第で移行、一時金と当面併存）。
10. **医療的判断を返さない。** 画面には常に「最終確認は窓口・医療機関へ」を添える。

## 4. 画面

```
[トップ]  /  安心・安全を「事実」で示す（出典と確認日／病院画面に広告なし／名前不要／医療判断なし）。上のタブは Tsugiraku Navi／記事／コンタクト（2026-09-22）
[入口]  /navi  住所（市区町村）／出産予定日／希望（無痛〈24時間対応の希望も〉・自宅の郵便番号・距離）／手元にある紙（複数選択）
   └ 選べる紙は documents マスタ。区の「母と子の保健バッグ」（includes を持つ紙）を一番最初に置き、次に「まだ紙がない」「その他」。残りは段階ごとのタブで、紙の名前を押すと説明が出る（2026-09-23）。
   └ 郵便番号は任意。時間の目安（直線距離、時速10km換算）を出すためだけに使い、端末の中にだけ保存する。サーバーには区の位置表（/api/postal?region=）を取りに行くだけ（2026-09-22）

[今週やること]  steps を解決し、未完了を期限順に表示。各行に 出典・確認日・完了チェック。並びは 次にやること → 終わったもの → このあと。
   └ 各行に「申請期間」（申請できる: ◯◯から／申請期限: ◯月◯日まで＋根拠の文章）を目立たせて出す。申請できる日は `apply_from_base/offset/week/note`（期限と同じ基準）。無い行は条件の紙から「◯◯を受け取ったあと」（lib/apply-window.ts、2026-09-23）。
   └ 各行に窓口の分別（区役所／東京都／国／勤務先・健康保険／医療機関）。channel の言葉から見分ける（lib/apply-to.ts）。「このあと」は窓口で絞れる。
   └ 「完了した」「自分は該当しない」「わからない」。「わからない」は理由（選択式）を聞き、同意があれば stuck_reports に送る（どこでつまずくかの集計。pnpm stuck:report）。一覧からは消さない（2026-09-23）。
[病院と締切]    facilities を地図＋一覧で。施設の公表締切週 と 記録の集計（予定月×電話した週×結果）。
   └ 予約を試した施設の記録は、同意の有無にかかわらず端末のアーカイブ（ninshin-navi:archive）に全部残す。妊娠ごと（予定日ごと）にまとめ、次の妊娠でも読める（2026-09-23）。
[お金]          出産なびの費用 − 一時金 − 都府県・市区町村の助成 = 実負担。scheme別。申請期限つき。
[製品]（段階4以降）product_reports の集計のみ。PR表記。病院画面から遷移させない。
[負担軽減コラム]  /articles  content/articles/<slug>/<lang>.md（ja／en）。読みもの。数字には出典と確認日。周期 `stage`（all/early/mid/late/birth/postpartum）で並べ、絞れる。製品・アフィリエイトリンクを含むコラムは `pr: true` と `programs`（amazon 等）が必須で、表示側が本文より前に「PR・広告を含みます」と規約の文言を言語ごとに出し、外部リンクに rel="sponsored" を付ける（ステマ規制）。病院画面からコラムへは遷移させない。アフィリエイトは運営者の判断で開始（2026-09-23。弁護士確認は後回し）
[コンタクト]  /contact  info@tsugiraku.jp への mailto と、名前なしの「まちがいを知らせる」。新しい問い合わせフォームは作らない
```

言語（2026-09-23 更新）: 日本語＋英語・中国語（簡体字）・韓国語・ロシア語（`lib/i18n.ts` の LANGS）。いまある範囲は ①トップページ（/en /zh /ko /ru）と ②コラム（同じ slug に言語ごとの .md）。③画面の文言 → ④手続きデータ本文は今後。④は翻訳文も出典・確認日つきのデータとして人が承認してから投入する。機械翻訳をそのまま画面に出さない。

収益（2026-09-22 方針）: 記事内のアフィリエイトと、リンクのまとめ（セット売りA。在庫を持たない）。仕入れて発送する物販（§7）は行わない。

### Next Action の解決手順（決定的）

1. `profiles.region_code` から地域の階層を得る: 市区町村 → 都府県 → `JP`。
2. 3階層の `steps` を読み、下位が `overrides_step_id` で指す上位ステップを除外する。
3. `trigger_document_id` が null か、`held_documents` に含まれるステップだけを候補にする。紙が別の紙を含む場合（`documents.includes`。例: 保健バッグ → 母子手帳・受診票）は、中の紙も持っている扱いにする。
4. 出産前（`profiles.birth_date` が未入力）は、`phase` が `birth`／`postpartum` のステップを隠す。出産後は、妊娠中にしかできないステップ（期限が妊娠週数で決まるもの、期限が出産予定日までのもの。例: 妊婦健診・妊婦面接）を隠す（2026-09-20 追加。隠さないと、出産後に「期限切れ」として出生届より前に出てしまう）。
3b. `requires`（`multiple`＝双子以上／`satogaeri`＝里帰り出産／`foreign_parent`＝子が日本国籍にならない）があるステップは、家族の状況（入口の「3. 希望」）に合うときだけ出す（2026-09-25）。
5. `step_progress` で完了済み（`done`）と「自分は該当しない」（`not_applicable`）を除外する。該当しないを付けたときはアンケートを出さない。
6. 期限を計算する。期限が30日以内（または過ぎている）ものを、期限の早い順で先頭に置く。それ以外は手続きの流れの順（`sort_order`）。ずっと先の期限が、日付のない急ぎの手続きより前に来ないようにするため（2026-09-19 変更）。
7. 地域の `status` が `verified` でなければ、画面上部に「この市区町村の情報は確認中です」を表示する。
8. 並べた先頭の1件を「次にやること（Next Action）」として強調し、なぜ先頭なのか（期限が近い／期限を過ぎている／手続きの順番）を添える。残りは「このあと」として控えめに表示する（One by One）。完了チェックはどれからでも付けられ、順番は強制しない。

### 期限の計算

- 最終月経開始日（LMP）= 出産予定日 − 280日。妊娠週数 = floor((今日 − LMP) ÷ 7)。
- `deadline_base`: `confirmation_date`（心拍確認日）／`notification_date`（母子手帳の受取日＝held_at）／`due_date`／`birth_date`／`gestational_week`（`deadline_week`）／`facility`（施設ごと）。
- 心拍確認日が未入力なら LMP＋49日で推定し、画面に「推定」と表示する。

## 5. 技術構成

- Next.js（App Router, TypeScript）＋ Tailwind、スマホ優先のPWA（アプリは作らない）
- Supabase（リージョン: 東京）: Postgres ＋ Auth（メールのマジックリンク。氏名は取らない）
- ログインなしで使える（2026-09-19 決定）。入力・完了チェックは端末のブラウザ内にだけ保存する。ログイン（マジックリンク）は製品の購入など段階4以降で使う
- メール通知は希望する人だけ（2026-09-19 決定）。確認メールのリンクで有効化。登録した人の分だけ、メールアドレスと期限計算に必要な入力をサーバーで預かる。週1回まで・広告なし。送信は Resend、実行は Vercel の週次 cron（`/api/cron/notify`）
- 記録（任意の1問）もログインなし。同意した場合だけ、サーバー（service_role）が保存する。重複防止は `reporter_hash = sha256(端末ごとのランダムID + REPORTER_HASH_SECRET)`。`/privacy` で同意の取り消しと記録の削除ができる
- Vercel でホスティング。週1回のcronで `source_watch` を実行。月1回のcronで保存期間（2年）を過ぎた行を消す（`retention_run()`、`/api/cron/retention`）
- 制度データ: `data/` のYAMLが正。`pnpm seed` でSupabaseへ投入（上書き）。差分はgitで追う
- 変更監視: `source_url` を取得しハッシュ比較 → 変更で `needs_review=true` → Claude API（Haiku）が差分を要約 → **人が承認してからYAMLを更新**
- 決済・紹介: Stripe／アフィリエイト（段階4以降。弁護士確認後）

## 6. UI規約

- 本文は16px以上。タップ領域は44px以上。
- 赤を使わない。期限の注意は琥珀色、完了は緑、情報は青。
- 大学生でもわかる言葉。専門用語・略称は初出で説明する。
- すべての金額の横に「出典・確認日」リンク。文字は「出典・◯◯区のページへ飛ぶ」（lib/source-label.ts。区・都・国以外は「出典・施設のページへ飛ぶ」）。

## 7. やらないこと

- スマホネイティブアプリ／AIチャットによる医療回答／健診結果などの健康データ保存
- 自由投稿の掲示板／写真アップロード／在庫を持つ物販／病院からの掲載料（医療広告規制のため要法務確認）

## 8. ディレクトリ

```
CLAUDE.md
docs/ROADMAP.md
supabase/schema.sql
data/national.yaml               # 国（JP）の骨格
data/prefectures/13.yaml         # 東京都
data/municipalities/13112.yaml   # 世田谷区
data/facilities/13112.yaml       # 世田谷区の分娩施設
data/surveys.yaml                # 完了チェック後の1問
data/reference/postal-13.json    # 郵便番号 → 位置（scripts/build-postal.ts が作る。pnpm build:postal）
content/articles/<slug>/{ja,en}.md  # 記事（docs/ARTICLES.md の決まりで書く）
content/legal/                   # 利用規約・プライバシーポリシーの下書き
app/ lib/ components/ scripts/   # Claude Code が作成
```

## 9. 地域を1つ追加する手順（段階2以降の型）

1. `data/municipalities/<5桁コード>.yaml` を作る。`region.status: in_progress`。
2. 自治体HPで、紙の名前・窓口・独自助成・申請期限を確認し、各行に `source_url` と `verified_at` を付ける。
3. 国・都府県と重なるステップは `overrides_step_id` で上書きする。
4. `needs_review: true` が0件になったら `status: verified` にする。
5. 3つのテスト家族（妊娠6週・妊娠20週・出産後2週）で「今週やること」を目視確認する。
6. 分娩施設は `data/facilities/<5桁コード>.yaml`（出産なびから転載、都府県の対象医療機関一覧、施設HPの予約ルール）。
7. `pnpm seed` → `pnpm watch:run`（新しい出典の基準を覚えさせる）。

書き方の決まり（2026-09-19、港区 13103 で試して確認）:
- 袋の中の紙は `documents.includes`。国の紙（`jp.boshi_techo`・`jp.kenshin_ticket`）も必ず入れる。国のステップがそれを条件にしているため。
- 助成額が定額でないときは `amount_formula`（`lib/formula.ts`）。使えるのは 数字・`+ - *`・かっこ・`min()`・`max()` と、変数 `children`（人数）・`cost`（出産なびの費用）・`lumpsum`（窓口で差し引かれる額の合計）。例: 港区の出産費用助成 `min(cost, 810000 + 480000 * (children - 1)) - lumpsum`。
- 助成の種類は `kind`（`at_counter`／`cash_later`／`recurring`／`conditional`）。商品券やギフトのように現金でないものは、実負担の計算に入れない（ステップの説明に書く）。

## 9.5 出典ページの変更が見つかったとき（変更監視の承認手順）

週1回（月曜 8:00）、`v_source_urls` にある出典URLを取りに行き、本文のハッシュを前回と比べる（`/api/cron/source-watch`。手元では `pnpm watch:run`）。

1. 変わっていたら、そのURLを出典にしている行に `needs_review = true` を付ける（画面に「内容を確認中」）。404 も同じ扱い。
2. `pnpm watch:report` で、どの行に影響するか・消えた行と増えた行・Haiku の要約（参考情報）を見る。
3. **人が**ページを開いて確かめ、`data/` のYAMLを直す。内容に変更が無ければ `verified_at` だけ新しくする。
4. `pnpm seed`。そのURLを出典にしている**すべての行**の `verified_at` が変更日以降になると、確認待ちが外れる。それまでは seed を流しても「内容を確認中」は消えない。
5. PDF は中身の比較ができない（差し替えに気づくだけ）。JavaScriptで表示するページ（e-Gov など）は変更に気づけないので、`pnpm watch:report` の「取得できていない」を時々見る。

## 10. 用語

- 一時金: 出産育児一時金（原則50万円、直接支払制度で施設に支払われる）
- 新制度: 正常分娩の費用を公的保険で賄う制度（改正健康保険法、2026年5月29日成立）
- ネウボラ面接: 世田谷区の妊娠期面接・8か月時面談・産後面接
- 出産なび: 厚生労働省の分娩施設検索サイト（転載時は出所明記）
